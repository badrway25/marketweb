# Summary · Cornice Public Flip · 2026-05-01

Cornice (`cornice-architettura`) flipped from `tier=draft` to `tier=published_live` after the user provided the explicit parallel-verification handshake reserved by workflow D §6 / R-SOL-8 / SOP §5.4. The cascade documented in workflow D `release-gatekeeper.md §6` was applied verbatim: 1 registry edit on `TEMPLATE_REGISTRY.json` (Cornice row · `tier` flipped + `tier_reason` rewritten to consolidate workflow A.5 → A.6 → C → D + flip audit trail), 1 management-command sync (`python manage.py sync_template_tiers` reports `cornice-architettura: draft -> published_live` and lands the catalog at 24 published_live / 0 draft), 7 explicit-literal tier-fact test bumps in `apps/catalog/tests.py` (23 → 24 across L824, L862, L868, L1134, L1554, L1556 plus `"23+" → "24+"` at L1141), and 1 anticipated negative-test re-binding (`test_pragma_falls_back_gracefully` default `limit=3 → limit=4` because Cornice's promotion pushed Elevate past the 3-slot ceiling in Pragma's category-fallback pool — the test's intent of validating the layered fallback is preserved). Zero edits to `apps/editor/`, `apps/projects/`, `apps/commerce/`, the 5 `template_content_cornice*.py` locale modules, the corporate-suite chrome partials, the home template, the views, the seed command, or the migrations. The home trust-counter rendered `24+` automatically via `WebTemplate.objects.filter(tier=PUBLISHED_LIVE).count()` (no template/view edit needed). Full Django test suite at **545/546** with the single failure being the documented pre-existing Continua-related booking-flag test (zero new regressions). Anonymous live verification on `http://127.0.0.1:8052/`: 45/45 anonymous routes 200 (5 locales × 5 pages + 5 locales × 4 case-detail), home counter `24+`, business catalog header `6 template disponibili` (was 5) with Cornice slug × 5 in HTML, AR locale serves `html lang="ar" dir="rtl"` with Naskh h1 + Amiri body still applied anonymously, frozen siblings Pragma/Fiscus/Solaria/Continua all 200 anon, and Continua's LF-5 AR h1 still computes to `Noto Kufi Arabic` (zero Naskh leakage proving Cornice's swap remains selector-scoped to `body.cs-lf-lf-2`). The corporate-suite cluster now ships 5 live siblings with all 5 layout-family slots populated (Pragma LF-1 + Cornice LF-2 + Fiscus LF-3 + Solaria LF-4 + Continua LF-5). Server left running at port 8052. The X.5 Cornice pipeline is closed.
