# Summary · Continua Pass B Multilingual · 2026-04-30

**Status**: GREEN · review-ready · `tier=draft` preserved
**Branch**: `phase-x4-continua-passB-multilingual-lf5`
**Aggregate**: 5.0 / 5 · 70 / 70 walk cells · 0 BLOCKING · 1 STRONG closed

Continua's LF-5 IT layout was rolled out to four additional locales (EN · FR · ES · AR) on 2026-04-30. The voice anchor `La continuità di una famiglia si misura in <em>generazioni</em>` is preserved verbatim-in-translation across all 5 locales — the load-bearing italic-em moves with the equivalent temporal noun (`generations` · `générations` · `generaciones` · `الأجيال`) in every language. The corporate-suite chrome's existing `dir="rtl"` + Noto Kufi heading + Amiri body swap pipeline picks up Arabic transparently; one STRONG finding emerged mid-walk — the LF-5-specific eyebrow surfaces (cycle cell eyebrows, pillar matrix numbers, sectors whistleblowing strip, leadership stations, timeline row eyebrows + horizon strongs, hero meta-strip, hero credit-row, hero anchor eyebrow) were absent from the original CS-TYPE-05 RTL letter-spacing reset list, so Arabic uppercase tracking still showed LTR artifacts. Fixed in `_base.html` by extending the existing reset rule additively (RTL-scoped only, so IT/EN/FR/ES bytes preserved); AR re-walked clean. The Pexels-only imagery pool is reused unchanged across every locale via `_POOL_*` constants extracted from the IT module — cross-locale substitution is structurally impossible. Tier remains `draft` in DB and registry — public flip is held for the user-handshake gate (workflow D · cluster R-SOL-8 / D-102 cadence). Continua is now ready for human visual review across IT · EN · FR · ES · AR with RTL parity, with 7 captures + 6 instrumented walks + 45/45 HTTP-200 smoke filed under `factory/reports/scorecard/continua-passB-multilingual/`.
